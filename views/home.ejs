<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YapSpace - Home</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        @font-face {
            font-family: 'Quartzo';
            src: url('https://fonts.googleapis.com/css2?family=Poppins:wght@700;800;900&display=swap');
            font-weight: 700;
            font-display: swap;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #140b29;
            color: white;
            min-height: 100vh;
        }

        /* Navbar Styles */
        .navbar {
            position: sticky;
            top: 0;
            width: 100%;
            background: rgba(20, 11, 41, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 16px 0;
            z-index: 1000;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 24px;
        }

        .logo {
            font-family: 'Quartzo', 'Poppins', sans-serif;
            font-size: 28px;
            font-weight: 800;
            color: white;
            text-decoration: none;
            letter-spacing: -0.5px;
        }

        .nav-menu {
            display: flex;
            align-items: center;
            gap: 32px;
            list-style: none;
        }

        .nav-item {
            position: relative;
        }

        .nav-link {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-weight: 500;
            font-size: 16px;
            transition: color 0.3s ease;
            padding: 8px 0;
        }

        .nav-link:hover {
            color: #8b5cf6;
        }

        /* Dropdown Styles */
        .dropdown {
            position: relative;
        }

        .dropdown-toggle {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            color: rgba(255, 255, 255, 0.8);
            font-weight: 500;
            font-size: 16px;
            transition: color 0.3s ease;
            padding: 8px 0;
        }

        .dropdown-toggle:hover {
            color: #8b5cf6;
        }

        .dropdown-arrow {
            transition: transform 0.3s ease;
        }

        .dropdown.active .dropdown-arrow {
            transform: rotate(180deg);
        }

        .dropdown-menu {
            position: absolute;
            top: 100%;
            right: 0;
            background: rgba(20, 11, 41, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 12px 0;
            min-width: 160px;
            opacity: 0;
            visibility: hidden;
            transform: translateY(-10px);
            transition: all 0.3s ease;
        }

        .dropdown.active .dropdown-menu {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .dropdown-item {
            display: block;
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            padding: 12px 20px;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .dropdown-item:hover {
            color: #8b5cf6;
            background: rgba(139, 92, 246, 0.1);
        }

        /* Main Content */
        .main-content {
            padding: 80px 24px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .cards-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 32px;
            max-width: 1000px;
            margin: 0 auto;
        }

        .card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(139, 92, 246, 0.2);
            border-color: rgba(139, 92, 246, 0.3);
        }

        .card-image {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
            color: white;
        }

        .card-content {
            padding: 24px;
        }

        .card-title {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 12px;
            color: white;
        }

        .card-description {
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .card-button {
            background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
            border: none;
            border-radius: 12px;
            color: white;
            padding: 12px 24px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .card-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.4);
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .nav-container {
                padding: 0 16px;
            }

            .nav-menu {
                gap: 20px;
            }

            .logo {
                font-size: 24px;
            }

            .nav-link, .dropdown-toggle {
                font-size: 14px;
            }

            .cards-container {
                grid-template-columns: 1fr;
                gap: 24px;
            }

            .main-content {
                padding: 60px 16px;
            }
        }
    </style>
</head>
<body>
    <!-- Sticky Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="#" class="logo">YapSpace</a>
            
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="#" class="nav-link">Home</a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link">Write a Blog</a>
                </li>
                <% if (locals.user) { %>
                    <li class="nav-item dropdown">
                        <div class="dropdown-toggle" onclick="toggleDropdown()">
                            <%= user.name %>
                            <span class="dropdown-arrow">▼</span>
                        </div>
                        <div class="dropdown-menu">
                            <a href="/user/logout" class="dropdown-item">Log Out</a>
                        </div>
                    </li>
                    <% } else { %>
                    <li class="nav-item dropdown">
                        <div class="dropdown-toggle" onclick="toggleDropdown()">
                            UserName
                            <span class="dropdown-arrow">▼</span>
                        </div>
                        <div class="dropdown-menu">
                            <a href="/user/singup" class="dropdown-item">Create Account</a>
                            <a href="/user/login" class="dropdown-item">Log In</a>
                        </div>
                    </li>
                    <% } %>
            </ul>
        </div>
    </nav>
 

    <!-- Main Content -->
    <main class="main-content">
        <div class="cards-container">
            <!-- Card 1 -->
            <div class="card" onclick="viewCard(1)">
                <div class="card-image">
                    📝
                </div>
                <div class="card-content">
                    <h3 class="card-title">Latest Articles</h3>
                    <p class="card-description">Discover the most recent blog posts and articles from our community writers.</p>
                    <button class="card-button" onclick="event.stopPropagation(); viewCard(1)">View</button>
                </div>
            </div>

            <!-- Card 2 -->
            <div class="card" onclick="viewCard(2)">
                <div class="card-image">
                    🚀
                </div>
                <div class="card-content">
                    <h3 class="card-title">Trending Topics</h3>
                    <p class="card-description">Explore the hottest topics and discussions happening right now in our community.</p>
                    <button class="card-button" onclick="event.stopPropagation(); viewCard(2)">View</button>
                </div>
            </div>

            <!-- Card 3 -->
            <div class="card" onclick="viewCard(3)">
                <div class="card-image">
                    👥
                </div>
                <div class="card-content">
                    <h3 class="card-title">Community Hub</h3>
                    <p class="card-description">Connect with fellow writers and readers in our vibrant community space.</p>
                    <button class="card-button" onclick="event.stopPropagation(); viewCard(3)">View</button>
                </div>
            </div>
        </div>
    </main>

    <script>
        function toggleDropdown() {
            const dropdown = document.querySelector('.dropdown');
            dropdown.classList.toggle('active');
        }

        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.querySelector('.dropdown');
            if (!dropdown.contains(event.target)) {
                dropdown.classList.remove('active');
            }
        });

        // Add scroll effect to navbar
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(20, 11, 41, 0.98)';
            } else {
                navbar.style.background = 'rgba(20, 11, 41, 0.95)';
            }
        });
    </script>
</body>
</html>